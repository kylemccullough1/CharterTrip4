namespace CharterTrip.Web.Auth;

/// <summary>The one role there is. Named rather than typed as a literal in three places.</summary>
public static class TripRoles
{
    public const string Admin = "admin";
}
